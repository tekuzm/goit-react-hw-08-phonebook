// ========== styles =========

import css from './HomePage.module.css';

const HomePage = () => (
  <div className={css.homePageContainer}>
    <div className={css.homePageText}>Welcome to the Phonebook App!</div>
  </div>
);

export default HomePage;
